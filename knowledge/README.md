# 知识库挂载目录

把业务规范、历史方案、SOP、数据字典、系统说明等文本文件放到这里，即可作为本地知识库使用。

支持格式：

- Markdown：`.md` / `.markdown`
- 文本：`.txt` / `.log`
- 结构化文本：`.json` / `.csv` / `.yaml` / `.yml`

在「设置 → 知识库挂载」中启用本地知识库。也可以填写其他本机目录的绝对路径。

当前本地检索是零依赖关键词检索，适合开发和小规模试点；当资料量增大时，可把 provider 切换为 `ragflow` 或 `http`，业务主流程无需改动。
